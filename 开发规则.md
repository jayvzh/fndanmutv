# DanmuTV・动漫弹幕刮削 开发规则（精简版 v1.0）

> 给 AI 的第一份规则。日常任务先执行本页即可，能省 token 就不必打开完整总则。
> 完整总则：[docs/DEVELOPMENT_RULES.md](docs/DEVELOPMENT_RULES.md)（架构、范围判断、交付完整自检时读；冲突以它为准）。
> 宗旨：先查文档、少扫描、小步改、不超范围。

## 0. 项目一句话
NAS 自托管的动漫本地弹幕刮削器（原 MoviePilot V2 插件抽离）。后端 FastAPI+APScheduler+SQLite（Python 3.12，conda 环境 `danmu`）；前端 Vue3+Vite+Vuetify3 纯 SPA；Docker 一条 compose 内置 danmu-api 部署。

## 1. 文档地图（按任务点开对应文档，禁止整目录通读）
| 任务 | 打开 |
| --- | --- |
| 判定需求范围 / 非目标 / 业务规则 | docs/PRD.md |
| 架构 / 刮削状态机 / 限流重试 / 定时任务 | docs/ARCHITECTURE.md |
| 目录组织 / 模块边界 / 数据流 | docs/PROJECT_STRUCTURE.md |
| 建表 / 迁移 / 配置项 | docs/DATA_MODEL.md |
| 接口、响应格式、错误码 | docs/API.md（唯一契约来源） |
| 页面 / 交互 | docs/UI_DESIGN.md |
| 背景现状 / 选型原因 / 踩坑 / 约定 | docs/context.md、docs/decisions.md、docs/pitfalls.md、docs/conventions.md |
| 本地开发 / ffmpeg / 部署细节 | docs/CONTRIBUTING.md |

## 2. 铁律（永久生效，违反即返工）
- **后端零 MoviePilot 依赖**：禁 `from app...`、`MetaInfo`、`MediaChain`、`save_data/get_data`、`schemas.Response`、`app.log` 等；插件逻辑只许在 `danmu_generator.py` 引擎内保留改造。
- 持久化只用标准库 sqlite3（[database.py](backend/app/database.py)，不引 ORM）；定时任务只注册 [scheduler.py](backend/app/services/scheduler.py) 的唯一 BackgroundScheduler，禁各处 new scheduler。
- API 统一 `/api` 前缀 + Bearer Token（401 缺 Token / 403 Token 错）；契约不破坏——`GET /config` 返裸 dict、`POST /clean_orphan_subtitles` 收裸数组，其余 `ApiResponse{success,message,data}`。
- 前端链路唯一：页面 → [src/api/index.js](frontend/src/api/index.js)，禁组件直接 fetch；禁留 `props.api`、postMessage/iframe、Module Federation/`remoteEntry.js`。
- 代码位置：后端 `backend/app/`，前端 `frontend/src/`；`references/` 只读不改；生成物与运行数据（data/、*.db、dist、node_modules、.env）不提交。
- 弹幕/字幕写回视频同目录；"有效弹幕"= `.danmu.chs.ass` 存在且 `Dialogue:` 行 ≥ 100；退避/限流下期、扫描深度 6 层、媒体扩展名白名单等业务数值以 docs/PRD.md §业务规则 为准，禁随手调。
- 表结构迁移只增不改；新增依赖必须同步 requirements.txt / package.json / Dockerfile。

## 3. 省 token 的行为约束
- 动手前先查 docs/ 与已有实现；检索限定 `backend/app`、`frontend/src`、`references/.../danmutv`，禁止无目标全局扫描。
- 文件 >1500 行用关键词检索 + 分段读（danmu_generator.py / danmu_service.py / BrowseView.vue / Config.vue），同一查询不重复执行。
- 改动最小化、用精确小 diff；不做 spec 外改动、不无意义重构；需求含糊先问清，禁止盲目大批生成。
- 注释精简；回复先给结论 + 必要片段；接口/表/配置变更双端同步并回写 API.md / DATA_MODEL.md / README。
- 项目独有知识及时沉淀 docs/（架构→context、选型→decisions、踩坑→pitfalls、约定/技术债→conventions），不写通用常识。

## 4. 常用命令（免翻脚本）
- 日常开发：`./dev.sh start|stop|restart|status|logs [backend|frontend]`。前端 http://localhost:8017（/api 代理到后端），后端 http://localhost:8021；conda 环境 `danmu` 由脚本自动激活，勿再手搓长命令。
- 后端冒烟（任何 python/pip 都走 conda）：`conda run -n danmu python -c "import app.main"`（在 backend/ 下）。
- 前端构建自检：`cd frontend && npm run build`——dist 正常、无 remoteEntry.js、CSS 含 `.v-`/`.mdi-`。
- 接口鉴权自检：curl 无 Token 得 401、带 `Authorization: Bearer <TOKEN>` 得 200。
- 涉 Docker 才 `docker compose build`（或 standalone）；日常功能验证禁止反复 build 镜像。

## 5. 交付自检（10 秒）
- 改动对应 spec Requirement、未超范围？tasks.md/checklist.md 已勾选？
- 位置正确（backend/app 或 frontend/src）、未碰 references/、无 MoviePilot 残留？
- 契约未破坏（裸 dict / 裸数组两个特例）？前端无 props.api/postMessage/iframe/remoteEntry？
- 后端冒烟导入过、前端 build 过、接口 401/200 过（涉 Docker 时 compose build 过）？
- 依赖已同步、无硬编码密钥、项目知识已回写 docs/？
- 完整清单：docs/DEVELOPMENT_RULES.md §10。
